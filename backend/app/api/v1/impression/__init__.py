"""The read Ally forms of a founder immediately after onboarding.

Onboarding collects thirteen answers. This turns them into something a founder
recognises as being about *them* -- the "Reading between the lines" screen and
the two that follow it.

Generated once per founder and stored (see founders.first_impression): an
impression that rewrites itself on every visit is not an impression, and it
would cost a model call per page view.
"""
