# app.goxlally.ai -- the GoXL Ally product, for founders who have been let in.
# The public site is www.goxlally.ai; that is what search engines should show.
# Crawling stays allowed so that every page's noindex is actually seen and the
# old entries for this domain drop out of the index. Nothing here is listed.

User-agent: *
Allow: /
Disallow: /api/
